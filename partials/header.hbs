<header class="site-header container">
    <div class="header-left">
        <!-- Raw
        <a class="logo" href="{{@site.url}}">
            <span class="logo-text">{{@site.title}}</span>
        </a> 
        -->

        <!-- Style 1
        <a class="logo r-link ai-element ai-element_type1 ai-element1" href="{{@site.url}}">
            <span class="logo-text ai-element__label">{{@site.title}}</span>
        </a>
        -->

        <a class="logo r-link ai-element ai-element_type3 ai-element9" href="{{@site.url}}">
            <span class="logo-text ai-element__label">{{@site.title}}</span>
        </a>
    </div>
    <div class="header-right">
        {{#if @site.navigation}}
            <nav class="main-nav hidden-xs hidden-sm hidden-md">
                {{navigation}}
            </nav>
        {{/if}}
        <div class="header-subscribe">
            <button
                class="button header-button header-button-subscribe js-popup">Subscribe</button>
            {{> customize/subscribe is_popup=true}}
        </div>
        {{#if @site.navigation}}
            <div class="header-menu hidden-lg hidden-xl">
                <button
                    class="button button-secondary header-button header-button-menu js-popup">
                    <i class="icon icon-menu"></i>
                </button>
                <div class="popup">
                    <div class="mobile-menu">
                        {{> navigation}}
                    </div>
                </div>
            </div>
        {{/if}}
    </div>
</header>